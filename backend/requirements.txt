# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.115.6
uvicorn[standard]>=0.32.1           # ASGI server (includes httptools + uvloop)

# ── Supabase client ───────────────────────────────────────────────────────────
supabase==2.10.0                    # Async v2 client (acreate_client)

# ── Validation & settings ─────────────────────────────────────────────────────
pydantic>=2.10.4
pydantic-settings>=2.7.0            # Reads from .env files

# ── HTTP transport — supabase 2.10.0 requires httpx <0.28 ────────────────────
httpx>=0.26,<0.28
